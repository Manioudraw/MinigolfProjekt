package src;

public class Player extends AbstractPlayer {
    public Player(String pName, char id) {
        super.setName(pName);
        super.setID(id);
    }

    public Player() {}

    void setMove() {
        // TODO: Hier fehlt noch was lol
    }
}
